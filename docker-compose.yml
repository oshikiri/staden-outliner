services:
  app:
    build:
      context: .
      target: dev
    command: bun run dev
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      STADEN_ROOT: /app/docs
      NODE_ENV: development
      HOST: 0.0.0.0
    volumes:
      - .:/app
      - app_node_modules:/app/node_modules
    tty: true
    stdin_open: true

volumes:
  app_node_modules:
