- {{staden-query observableplot}}
	- ```js
	  const linedata = [
	    {hour: 0, value: 8, sensor: "A"},
	    {hour: 0, value: 6, sensor: "B"},
	    {hour: 1, value: 7, sensor: "A"},
	    {hour: 1, value: 5, sensor: "B"},
	    {hour: 2, value: 3, sensor: "A"},
	    {hour: 2, value: 0, sensor: "B"},
	    {hour: 3, value: 9, sensor: "A"},
	    {hour: 3, value: 2, sensor: "B"}
	  ];
	  return Plot.plot({
	    marks: [
	      Plot.lineY(linedata, {x: "hour", y: "value", stroke: "sensor"}),
	      Plot.tip(linedata, {
	        ...Plot.pointerX({
	  			x: "hour",
	  			y: "value",
	  			stroke: "sensor",
	  			title: (d) => `${d.sensor}: ${d.value}`
	  		}),
	      })
	    ]
	  })
	  ```
- {{staden-query}}
	- ```sql
	  select page_title, count(1) as count from blocks_p group by page_title
	  ```
	- ```js
	  return Plot.plot({
	    marginBottom: 60,
	    x: {
	      tickRotate: -30,
	    },
	    marks: [
	      Plot.ruleY([0]),

	      Plot.barY(results, {
	        x: "page_title",
	        y: "count"
	      }),
	    ]
	  })
	  ```
	-
