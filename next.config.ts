import type { NextConfig } from "next";

// RV: Consider enabling strict mode and configuring headers/security (e.g., CSP) in Next config.
const nextConfig: NextConfig = {};

export default nextConfig;
